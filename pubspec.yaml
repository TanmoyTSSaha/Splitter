name: splitr
description: "Splitr — shared expense management and personal finance."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  # appwrite: ^17.1.0 # Removed as unused
  neopop: ^1.0.2
  google_fonts: ^6.3.3
  get: ^4.6.6
  flutter_svg: ^2.0.10+1
  rxdart: ^0.28.0
  avatar_stack: ^1.2.0
  intl: ^0.19.0
  salomon_bottom_bar: ^3.3.2
  flutter_timeline: ^0.3.0
  fl_chart: ^0.68.0
  supabase_flutter: ^2.6.0
  loading_animation_widget: ^1.3.0
  fluttertoast: ^8.2.8
  cached_network_image: ^3.3.0

  # Phase 2: Swipe-to-Settle (haptic feedback)
  vibration: ^2.1.0

  # Phase 2: Micro-animations
  lottie: ^3.1.0

  # Phase 2: AI Receipt Scanner
  google_mlkit_text_recognition: ^0.13.0
  image_picker: ^1.1.1 # Unpinned

  # Phase 2: Offline-first (Drift local DB)
  drift: ^2.16.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.3 # Unpinned
  path: ^1.9.0

  # Phase 2: Smart reminders
  flutter_local_notifications: ^17.0.0

  # Phase 2: Connectivity detection for sync
  connectivity_plus: ^6.0.0
  shared_preferences: ^2.5.4

  # Phase 4: Shareable cards
  share_plus: ^10.1.4

  # Phase 4: Pro subscriptions (Razorpay via Supabase Edge Functions)
  # Phase 4: PDF export
  pdf: ^3.11.1

  # Phase 4: Biometric authentication
  local_auth: ^2.3.0
  google_generative_ai: ^0.4.7
  story_view: ^0.16.6
  screenshot: ^3.0.0
  flutter_contacts: ^2.1.0
  app_links: ^7.0.0
  uuid: ^4.5.3
  timezone: ^0.9.4
  google_sign_in: ^7.2.0
  razorpay_flutter: ^1.4.5
  flutter_dotenv: ^5.2.1
  receive_sharing_intent: ^1.8.1
  file_picker: ^8.1.7
  url_launcher: ^6.3.1
  sentry_flutter: ^9.24.0

# dependency_overrides:
#   # Removed manual overrides to avoid v1 embedding crashes
#   # We rely on downgrading main packages above

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.0

  # Phase 2: Drift code generation
  drift_dev: ^2.20.0
  build_runner: ^2.4.0
  sentry_dart_plugin: ^3.2.1

sentry:
  upload_debug_symbols: true
  upload_sources: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/icons/svg/
    - assets/dev_images/
    - assets/badges/
    - assets/lottie/
    - assets/fonts/pdf/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
    - family: Albra
      fonts:
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Thin.otf
          weight: 100
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Light.otf
          weight: 300
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Light-Italic.otf
          weight: 300
          style: italic
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Regular.otf
          weight: 400
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Regular-Italic.otf
          weight: 400
          style: italic
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Medium.otf
          weight: 500
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Medium-Italic.otf
          weight: 500
          style: italic
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Semi.otf
          weight: 600
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Semi-Italic.otf
          weight: 600
          style: italic
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Bold.otf
          weight: 700
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Bold-Italic.otf
          weight: 700
          style: italic
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Black.otf
          weight: 900
        - asset: assets/fonts/Albra Serif Font/AlbraTRIAL-Black-Italic.otf
          weight: 900
          style: italic
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
